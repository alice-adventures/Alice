name = "alice"
version = "0.4.2-dev"
description = "Adventures for Learning and Inspiring Coding Excellence"
website = "https://github.com/alice-adventures/Alice/wiki"

authors = ["Francesc Rocher"]
maintainers = ["Francesc Rocher <francesc.rocher@gmail.com>"]
maintainers-logins = ["rocher"]

licenses = "MIT"
tags = [
   "alice",
   "adventures",
   "learning",
   "mathematics",
   "problems",
   "challenges",
]

project-files = [
   "project/alice_lib.gpr",
   "project/alice_cli.gpr",
   #TODO - "project/alice_gui.gpr",
]

[available.'case(os)']
'linux' = true
'...' = false

[configuration.variables]
Log_Level = { type = "Enum", values = [
   "Default", # warnings and errors
   "Verbose", # more detailed logs (info)
   "Trace",   # trace execution
   "Debug",   # debug information
], default = "Default" }

[build-profiles]
# release, validation, development
"*" = "release"
"alice" = "development"

[build-switches."*"]
ada_version = ["-gnat2022", "-gnatX0", "-gnata"]

[[depends-on]]
ada_toml = "*"
clic = "~0.3.0"
curl = "*"
gnat = ">=2021 | (>=12 & <15)"
json = "^6.0.0"
mold_lib = "^2.3.1"
simple_logging = "^1.2.0"
spdx = "~0.2.0"
sha1 = "^2.0.0"
